# Agents 行为规范

## 目的
确保 Agents 在对本项目（PvP 匹配 Mod）进行代码修改时遵循严谨的流程，保证修改清晰、可追溯、且不引入回归。

## 基本流程
每次代码修改必须按以下步骤执行，缺一不可：

### 1. 说明修改原因
在开始修改之前，先在注释、提交信息或 issue 中明确说明 **为什么需要此改动**：
- 修复什么 bug？
- 添加什么功能？
- 改进什么性能或可读性？
- 依据哪些观察或测试结果？

### 2. 查看相关代码及上下文
- 阅读将要修改的文件及其周边代码，理解其职责和调用关系。
- 如果涉及多个文件，先梳理修改的影响范围。
- 必要时使用 `grep`、`IDE` 搜索或 `git log` 查看历史。

### 3. 审查修改内容（自查）
- 在实际编辑之前，先心理过一遍或写下伪造的 diff，确认修改是否符合预期。
- 检查是否有遗漏的边界情况、是否遵循项目现有编码风格（缩进、命名、注释等）。

### 4. 进行修改
- 使用工具（如编辑器）按审查好的内容完成代码更改。
- 每次修改应保持尽量小的粒度，便于后续审查。

### 5. 检查（Check）
- 确保没有语法错误：运行项目构建命令。
  ```bash
  ./gradlew build   # 或 ./gradlew clean build
  ```
- 检查是否产生新的警告或错误，若有则必须解决。
- 如有单元测务，运行相关测试以验证修改不破坏现有功能。

### 6. 测试（Test）
- 启动本地 Fabric 服务器（1.21.1），加载修改后的 mod。
- 基于修改的功能或模块进行手动或自动化验证：
  - 若是新特性：确保能够通过游戏内 GUI 或命令触发并表现正确。
  - 若是 bug 修复：复现原始问题，确认修改后问题消失。
  - 若是重构：确保核心玩法（如匹配、竞技场生成、战斗机制等）仍然正常。
- 测试过程中注意观察控制台日志，确保无异常堆栈。

### 7. 版本管理（Git）
- 修改并验证通过后，使用 `git status` 查看变动。
- 使用 `git diff` 审查实际更改，确保仅包含预期修改。
- 提交前确保提交信息格式清晰：
  ```
  <type>: <简要描述>

  <详细说明修改原因和主要变更>
  ```
  其中 `<type>` 可为 `feat`、`fix`、`refactor`、`docs`、`style`、`test`、`chore` 等。
- 提交后推送至远程分支（如适用）。

## 注意事项
- **不允许直接跳过原因说明和审查步骤**进行修改。
- 若修改跨越多个模块（如 arena、bedwars、kits），应分别对每个模块遵循上述流程。
- 任何修改后必须通过构建检查；构建失败不得提交。
- 鼓励在修改后编写或补充相关注释，尤其是复杂逻辑。
- 如不确定修改影响，可先在分支上实验，再通过 `git merge` 或 `pull request` 合入主分支。

## 示例提交信息
```
fix: 修复 BedWars 地图标记保存时的空指针异常

在编辑模式下，当玩家右键保存时，若未正确初始化商店实体列表，
会导致 NullPointerException。本修复在保存前进行空检查，
并确保列表在编辑模式进入时被正确填充。

- 阅读 BedWarsEditor.java 中的 save 方法
- 添加空检查并记录日志
- 通过本地服务器测试编辑、保存、重新加载地图的完整流程
- 构建通过，无新警告
```

遵守本规范，有助于保持代码质量和项目可维护性。
